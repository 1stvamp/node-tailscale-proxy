{
  "name": "@1stvamp/proxy",
  "version": "1.0.2",
  "description": "Proxy any URL via Tailscale Serve",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "patch-package",
    "patch-package": "patch-package"
  },
  "author": "Wes Mason <wes@1stvamp.org>",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.18.1",
    "patch-package": "^8.0.0",
    "portfinder": "^1.0.32"
  },
  "bin": {
    "proxy": "./index.js"
  },
  "devDependencies": {}
}
